<UserControl x:Class="MiniRPG.Views.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <!-- VIEWBOX: scala automaticamente tutto il contenuto -->
    <Viewbox Stretch="Uniform">
        <Grid Width="800" Height="600" Background="Black">

            <!-- Area di gioco -->
            <Canvas x:Name="GameCanvas" Background="#222222" Margin="0,40,0,40"/>

            <!-- HUD -->
            <StackPanel Orientation="Horizontal" Margin="10" VerticalAlignment="Top">
                <TextBlock Text="   Monete: " Foreground="Gold" FontSize="16" Margin="20,0,0,0"/>
                <TextBlock Text="{Binding PlayerCoins}" Foreground="Gold" FontSize="16"/>
            </StackPanel>

            <TextBlock Text="{Binding CurrentLevel, StringFormat='Livello: {0}'}"
                       Foreground="White" FontSize="16"
                       HorizontalAlignment="Right" VerticalAlignment="Top"
                       Margin="0,10,10,0"/>
        </Grid>
    </Viewbox>
</UserControl>
